# Data Governance

- Keep raw data in `data/raw` and processed artifacts in `data/processed`.
- Track normalization and deduplication decisions in JSONL fields.
- Store dataset versions with checksums and document data sources.
